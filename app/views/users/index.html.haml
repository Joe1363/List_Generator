.container
  %h2.text-center= "Manage Lists"
  %br
  .text-center
    = link_to "Create New List", new_list_path, class: "btn btn-default"
  %br
  .row
    .col-md-6
      %h4.underline= "Title"
    .col-md-1
      %h4.underline= "Created"
    .col-md-1
      %h4.underline= "Edited"
    .col-md-2
      %h4.underline= "Options"
  - @user.lists.each do |a|
    .row
      .col-md-6
        %p= a.name
      .col-md-1
        %p= a.created_at.strftime("%m-%d-%y")
      .col-md-1
        %p= a.updated_at.strftime("%m-%d-%y")
      .col-md-2
        %p
          = link_to "Edit", edit_list_path(a.id)
          |
          = link_to 'Delete', "/lists/#{a.id}", data: {confirm: "Are you sure you want to delete this article?"} , method: :delete
